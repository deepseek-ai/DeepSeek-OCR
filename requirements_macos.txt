# DeepSeek-OCR Requirements for macOS (Apple Silicon)
#
# Install with: pip install -r requirements_macos.txt
#
# Note: This EXCLUDES vLLM and flash-attn which are CUDA-only

# Core ML libraries
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
transformers==4.46.3
tokenizers==0.20.3

# PDF processing
PyMuPDF>=1.23.0
img2pdf>=0.5.0

# Image processing
Pillow>=10.0.0

# Utilities
einops>=0.7.0
easydict>=1.10
addict>=2.4.0
numpy>=1.24.0

# Optional: For better progress bars and logging
tqdm>=4.65.0

# Optional: For Jupyter notebook support in VSCode
# jupyter>=1.0.0
# ipykernel>=6.25.0

# DO NOT INSTALL (CUDA-only):
# - vllm (requires NVIDIA GPU)
# - flash-attn (requires NVIDIA GPU)
